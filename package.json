{
  "name": "qr",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "npm run build && vite preview",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.11.2",
    "@types/qrcode": "^1.5.5",
    "vite": "^7.1.1",
    "wrangler": "^4.28.1"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
